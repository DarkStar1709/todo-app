name: todo_task
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.7.2

dependencies:
  cloud_firestore: ^5.6.7
  equatable: ^2.0.7
  firebase_auth: ^5.5.3
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.1
  go_router: ^15.1.2
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
